# Ensis Admin

Admin console for the Ensis frontend and backend.

## Run

```bash
npm install
npm run dev
```

The app runs on `http://localhost:3001` and talks to the backend from `NEXT_PUBLIC_API_URL`.
